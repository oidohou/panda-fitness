<nav class="navbar navbar-expand-lg navbar-dark bg-dark p-3">
  <%= link_to "Move & Groove", root_path , :class => 'navbar-brand text-uppercase' %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarText">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <%= link_to "Home", welcome_path ,class:"nav-link"%>
      </li>
      <li class="nav-item">
        <%= link_to "About", about_path,class:"nav-link" %>
      </li>
      <li class="nav-item">
        <%= link_to "Upcoming events", events_path,class:"nav-link" %>
      </li>
    </ul>
    <ul class="navbar-text navbar-nav">
    <% if user_signed_in?%>
      <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" 
          role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Welcome, <%= current_user.email.split('@')[0] %>
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <div class="dropdown-divider"></div>
            <%= link_to "My activities", activities_path %>
            <div class="dropdown-divider"></div>
            <%= link_to "My profile", profile_path %>
            <div class="dropdown-divider"></div>
            <%= button_to "Log out", destroy_user_session_path, :method => :delete, class:"btn btn-dark"%>
          </div>
      </li>    
    <% else %>
      <li class="nav-item mr-2">
        <%= link_to "Sign in", new_session_path(:user) %> | <%= link_to "Sign up", new_registration_path(:user) %>
      </li>
    <% end %>
    </ul>
  </div>
</nav>
