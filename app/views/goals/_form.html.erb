<%= form_with(model: goal, local: true) do |form| %>
  <% if goal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(goal.errors.count, "error") %> prohibited this goal from being saved:</h2>

      <ul>
      <% goal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field  row my-5 ">
    <%= form.label :ideal_Weight , {class:' text-right col-2'} %>
    <div class="col-10">
      <%= form.text_field :idealWeight, {class:'form-control'} %>
    </div>
  </div>

  <div class="actions col-12 p-2">
    <%= form.submit "Save Goal", class:" mx-5 btn btn-dark" %>
    <%= link_to 'Cancel', goals_path , class:" mx-5 btn btn-dark"%>
  </div>
<% end %>
